#include "npuzzle.h"

/*char	**get_solved(char **puzzle)
{
	int	len;
	int	i;
	int	j;
	char	*solved;
	int	value;

	len = ft_strlen(*puzzle);
	if (!(solved = (char**)malloc(sizeof(char*) * (len + 1))))
		return (NULL);
	i = 0;
	while (i <= len)
		solved[i++] = NULL;
	i = 0;
	value = 1;

}*/
